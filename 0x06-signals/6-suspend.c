#include "signals.h"

/**
 * main - sets a handler for the signal SIGINT,
 * and exits right after the signal is received and handled
 * Return: Exit with EXIT_SUCCESS
 */
int main(void)
{
	return (EXIT_SUCCESS);
}
